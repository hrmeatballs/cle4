export default class
{
    id:number;
    name:string;
    student_id:number;
    progress:string;

    constructor(id: number, name: string, student_id: number, progress: string) {
        this.id = id;
        this.name = name;
        this.student_id = student_id;
        this.progress = progress;
    }

    init():void
    {
        //Todo: Create initialize functions
    }
}